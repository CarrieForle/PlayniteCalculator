<UserControl x:Class="Calculator.SidebarView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:local="clr-namespace:Calculator"
			 mc:Ignorable="d"
			 d:DesignHeight="800" d:DesignWidth="1100">
	<UserControl.Resources>
		<Style TargetType="TextBlock" BasedOn="{StaticResource BaseTextBlockStyle}">
			<Setter Property="VerticalAlignment" Value="Center" />
			<Setter Property="Margin" Value="5" />
		</Style>
		<Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
			<Setter Property="VerticalAlignment" Value="Center" />
			<Setter Property="Margin" Value="5" />
		</Style>
		<Style TargetType="RadioButton" BasedOn="{StaticResource {x:Type RadioButton}}">
			<Setter Property="Margin" Value="5" />
			<Setter Property="VerticalAlignment" Value="Center" />
		</Style>
		<Style TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
			<Setter Property="Margin" Value="5" />
			<Setter Property="VerticalAlignment" Value="Center" />
		</Style>
		<Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
			<Setter Property="Margin" Value="5" />
		</Style>
		<Style TargetType="Label" BasedOn="{StaticResource {x:Type Label}}">
			<Setter Property="Margin" Value="5" />
		</Style>
		<Style x:Key="BigStat" TargetType="TextBlock">
			<Setter Property="FontSize" Value="24" />
		</Style>
		<Style x:Key="LabelStyle" TargetType="TextBlock">
			<Setter Property="Foreground" Value="#888888"/>
			<Setter Property="FontSize" Value="12"/>
			<Setter Property="FontStyle" Value="Italic"/>
			<Setter Property="Margin" Value="0,0,0,10"/>
		</Style>
		<Style x:Key="ValueStyle" TargetType="TextBlock">
			<Setter Property="Foreground" Value="White"/>
			<Setter Property="FontSize" Value="36"/>
			<Setter Property="FontWeight" Value="Light"/>
		</Style>
	</UserControl.Resources>
	<StackPanel>
		<TextBlock Text="Account Value" FontSize="28" HorizontalAlignment="Center"/>
			<WrapPanel Orientation="Horizontal" >
				<StackPanel Margin="30,0">
					<TextBlock Text="{local:MoneyBinding TotalSpentIfDiscountedPrice}" 
								   Foreground="#00ff00" FontSize="48" FontWeight="Bold"/>
					<TextBlock Text="lowest prices" Foreground="#888888" FontSize="12" HorizontalAlignment="Center"/>
				</StackPanel>
				<StackPanel Margin="30,0">
					<TextBlock Text="{local:MoneyBinding TotalSpentIfRegularPrice}" Foreground="#ff7b5a" FontSize="48" FontWeight="Bold"/>
					<TextBlock Text="Today's prices" Foreground="#888888" />
				</StackPanel>
			</WrapPanel>
		<WrapPanel VerticalAlignment="Bottom" Margin="0,40,0,0">
			<StackPanel>
				<TextBlock Text="Average price" Style="{StaticResource LabelStyle}"/>
				<TextBlock Text="{local:MoneyBinding AveragePrice}" Style="{StaticResource ValueStyle}"/>
			</StackPanel>
			<StackPanel>
				<TextBlock Text="Average price per hour" Style="{StaticResource LabelStyle}"/>
				<TextBlock Text="{local:MoneyBinding PricePerHour}" Style="{StaticResource ValueStyle}"/>
			</StackPanel>
			<StackPanel>
				<TextBlock Text="Total Playtime" Style="{StaticResource LabelStyle}"/>
				<TextBlock Text="{Binding TotalPlaytime, Converter={StaticResource PlaytimeConverter}}" Style="{StaticResource ValueStyle}"/>
			</StackPanel>
		</WrapPanel>
	</StackPanel>
</UserControl>